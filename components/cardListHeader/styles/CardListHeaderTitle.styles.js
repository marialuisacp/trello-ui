import styled from 'styled-components';
import { styles } from '../../../styles/config';

const StyledCardListHeaderTitle = styled.span`
  color: ${styles.colorText};
  font-size: ${styles.fontSizeDefault};
  font-weight: 700;
  height: auto;
  line-height: 24px;
`;

export default StyledCardListHeaderTitle;

