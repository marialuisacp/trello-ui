import styled from 'styled-components';

const StyledHeaderDivisor = styled.span`
  float: left;
  border-left: 1px solid hsla(0,0%,100%,.24);
  height: 16px;
  margin: 8px 4px 12px 4px;
`;

export default StyledHeaderDivisor;